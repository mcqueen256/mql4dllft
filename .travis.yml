language: cpp
sudo: false

compiler:
  - clang
  - gcc

os:
  - linux

python: 3.5.0

before_install:
  - pip install --user cpp-coveralls
install: true
before_script: true
script: scons
after_success:
  - ./mql.out
  - coveralls --exclude lib --exclude tests --gcov-options '\-lp'